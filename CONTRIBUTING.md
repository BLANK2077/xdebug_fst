# 贡献与问题反馈

请先阅读 README 和发布环境指南，使用锁定工具链和独立构建目录。不要重新生成未变更的 fixture，不要为通过测试修改冻结 schema、oracle 或语义基线。

提交前运行与修改有关的测试、`git diff --check` 和本机路径检查。接口变化需说明输入、输出、错误和兼容性影响；新增能力需提供能证明行为的测试。默认使用详细中文 commit，本项目不要求贡献者通过 PR 工作流。

只提交你有权贡献的内容，保留第三方原始版权和许可证。禁止提交 EDA 安装文件、NPI/FSDB/VIP 头库、商业手册、客户设计、license 文件或凭据。测试重放和工具生成文件同样需要核对来源。

反馈问题请提供：`xdebug-fst --version --json`、平台、最小请求、实际结果、预期结果和可公开的最小 RTL/FST。不要直接附带客户波形或未审查的大型原始日志。会话问题可附带已去除敏感路径的 `session.doctor` 结果。

公开发布前的许可证疑点按 THIRD_PARTY.md 单独跟踪，由拥有相关权利的人确认，不将自动扫描结果视为授权。
